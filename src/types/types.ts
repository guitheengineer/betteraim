// Used to type useState that holds window position.
export interface IPosition {
  x: number;
  y: number;
}

// Used to type useState that holds window dimensions.
export interface IDimensions {
  width: number;
  height: number;
}
